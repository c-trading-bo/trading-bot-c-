# TopstepX SDK Python Dependencies
# Install with: pip install -r requirements.txt

# Main SDK requirement as specified in problem statement
project-x-py[all]>=3.5.0

# Additional dependencies for production use (many included with SDK but explicit for clarity)
asyncio-throttle>=1.0.0
aiohttp>=3.8.0
websockets>=11.0.0
pydantic>=2.0.0
python-dateutil>=2.8.0
httpx>=0.27.0
orjson>=3.11.0
polars>=1.31.0
rich>=14.1.0
signalrcore>=0.9.5

# HTTP API Server for .NET integration
flask>=3.0.0
flask-cors>=4.0.0

# ML/RL Dependencies for Backtest Validation and Model Training
# Note: numpy version must match project-x-py requirement (>=2.3.2)
# pandas-ta will be installed without numba dependency
numpy>=2.3.2
pandas>=2.0.0
# pandas-ta with numba causes conflicts, so we'll install minimal version or skip numba
# pandas-ta>=0.3.14b0
yfinance>=0.2.28
ta-lib>=0.4.28
scikit-learn>=1.3.0