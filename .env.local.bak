TOPSTEPX_API_KEY=7IrbO/+ieWsRK83krOrNCPKuTFmD/5nkALj9WMZBEz4=
TOPSTEPX_ACCOUNT_ID=10223789
TOPSTEPX_USERNAME=kevinsuero072897@gmail.com

# Autopilot (zero manual input)
AUTO=true
AUTO_GO_LIVE=true
AUTO_MIN_HEALTHY_PASSES=3
AUTO_DRYRUN_MINUTES=0
AUTO_DEMOTE_ON_UNHEALTHY=3
AUTO_STICKY_LIVE=true


# Local run helpers
SKIP_CONNECTIVITY_PROBE=true
BOT_QUICK_EXIT=0
SKIP_MODE_PROMPT=1
BOT_MODE=shadow

# RTC hubs
RTC_USER_HUB=https://rtc.topstepx.com/hubs/user
RTC_MARKET_HUB=https://rtc.topstepx.com/hubs/market

# Symbols
TOPSTEPX_SYMBOLS=ES,NQ

# Verbose logging to debug MarketHub subscriptions
APP_CONCISE_CONSOLE=false


# Web host port (dashboard + health on the same port)
ASPNETCORE_URLS=http://localhost:5050


# Integrity enforcement (protect login/auth code). Do not change unless you intend to approve auth-plumbing edits.
INTEGRITY_ENFORCE=1
# Explicitly disable any runtime login (JWT acquisition) unless you flip this to 1/true/yes.
AUTH_ALLOW=1


# --- Tuning (historical optimizer) ---
# Provide a JWT to allow history fetches (required for tuning). Do NOT quote.
# NOTE: Don't commit real tokens. Prefer keeping this only on your machine.
TOPSTEPX_JWT=

# Enable tuner and choose what to run
RUN_TUNING=1
STRATEGIES=S3
SYMBOLS=ES,NQ
TUNE_LOOKBACK_DAYS=10
TUNE_SUMMARY_ONLY=1

# S3 strategy config override (absolute path). Also enable per-guard debug counters.
S3_CONFIG_PATH=c:\Users\kevin\Downloads\C# ai bot\state\tuning\S3-StrategyConfig.tuned.json
S3_DEBUG_REASONS=1

# Optional: set explicit contracts to skip auto-resolve (JWT still required)
# TOPSTEPX_CONTRACT_ES=
# TOPSTEPX_CONTRACT_NQ=
