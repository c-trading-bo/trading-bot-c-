<Project>
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <!-- TreatWarningsAsErrors staged rollout plan: -->
    <!-- Phase 1: Fix async method warnings (in progress) -->
    <!-- Phase 2: Fix nullable reference warnings -->
    <!-- Phase 3: Fix type conflict warnings -->
    <!-- Phase 4: Enable TreatWarningsAsErrors globally -->
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    <!-- Target: Enable once warning count reduced to <50 across all projects -->
  </PropertyGroup>
</Project>
