name: Sector Rotation Analysis
on:
  schedule:
  - cron: 0 23 * * 1-5
  workflow_dispatch: null
jobs:
  steps:
    runs-on: ubuntu-latest
    steps: []
  timeout-minutes:
    runs-on: ubuntu-latest
    steps: []
'on':
  workflow_dispatch: {}
