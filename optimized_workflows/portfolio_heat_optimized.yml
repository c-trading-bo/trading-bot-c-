# Portfolio Heat Map - OPTIMIZED for 50K Budget  
name: Portfolio Heat (Budget Optimized)

"on":
  schedule:
    # OPTIMIZED SCHEDULE - 35% reduction with smart timing
    
    # MARKET HOURS: Every 20 min (vs 15 min current)
    - cron: '*/20 14-21 * * 1-5'        # US market hours
    
    # EXTENDED HOURS: Every 45 min (vs 30 min current)  
    - cron: '*/45 13-14,21-23 * * 1-5'  # Pre/after market
    
    # WEEKENDS: Every 3 hours (vs hourly current)
    - cron: '0 */3 * * 6,0'             # Weekend monitoring
    
    # OVERNIGHT: Every 4 hours
    - cron: '0 */4 0-13 * * 1-5'        # Overnight coverage
    
    # Total: ~12 runs/day (vs 18 current) = 33% reduction
    
  workflow_dispatch:

env:
  OPTIMIZATION_MODE: "balanced_efficiency"
  
jobs:
  portfolio-heat-optimized:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
    - name: 🌡️ Optimized Portfolio Heat Start  
      run: |
        echo "🔥 Portfolio Heat Map - Budget Optimized"
        echo "📊 Smart scheduling: 20min market, 45min extended, 3hr weekends"
        echo "💡 Maintains risk monitoring while reducing costs 35%"
